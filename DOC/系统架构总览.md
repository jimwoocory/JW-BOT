# JW-Bot 系统架构总览

最后更新：2026-04-24

## 整体架构

```
员工（QQ / 飞书 / WebChat）
         ↓ 消息
    AstrBot 核心
    ┌─────────────────────────────────────────┐
    │  Pipeline                                │
    │  RouterStage → ProcessStage → RespondStage│
    │       ↓              ↓                   │
    │  意图分类       Star Handlers             │
    │  任务/技能/对话  LLM Agent               │
    └──────────┬──────────────────────────────┘
               │
     ┌─────────┴──────────┐
     │                    │
  Harness 系统        hermes_bridge
  任务全生命周期        ↕ Webhook
  记忆提升             Hermes Agent
```

## 消息处理 Pipeline

```
1. RouterStage     意图分类（rules + LLM fallback）
                   → task: 创建 Harness 任务
                   → skill: 激活 Star handler
                   → conversation: 透传

2. ProcessStage    Star handlers 处理（onboarding / builtin_commands / plugins）
                   → 如无 handler 结果，走 LLM Agent

3. InternalAgentSubStage   LLM 生成响应
                           → _maybe_complete_harness_task（自动闭环）

4. RespondStage    发送回平台
```

## 关键数据存储

| 文件 | 存储内容 |
|------|----------|
| `data/data_v4.db` | 会话历史、Persona、session 配置（sp preferences） |
| `data/lossless_context.db` | 无损压缩上下文（lossless_items / heads） |
| `data/harness.db` | Harness 任务记录 |
| `data/harness_memory.db` | LLM 响应提升的记忆 |
| `data/knowledge_base/kb.db` | 知识库元数据（文档/chunk 需通过 dashboard 上传） |
| `data/hermes_sessions.db` | hermes_bridge 的 platform_user → session_key 映射 |

## 已上线的插件

| 插件 | 位置 | 功能 |
|------|------|------|
| `onboarding_guide` | `data/plugins/onboarding_guide/` | 新员工 ABC 引导 + 人格自动绑定 |
| `hermes_bridge` | `data/plugins/hermes_bridge/` | AstrBot ↔ Hermes Webhook 桥接 |
| `dreamina_plugin` | `data/plugins/dreamina_plugin/` | 即梦 AI 图片/视频生成 |
| `builtin_commands` | `astrbot/builtin_stars/builtin_commands/` | /task 系列命令 |
| `session_controller` | `astrbot/builtin_stars/session_controller/` | 会话 LLM 开关控制 |

## 人格（Persona）配置

| Persona ID | 适用角色 | 备注 |
|------------|----------|------|
| `Biz_Assistant_Claw` | 推广/运营/内容 | 默认推荐 |
| `Enterprise_Ops_Kernel` | 项目管理/跟进 | |
| `DevOps_Console` | 技术/运维 | |

人格存储在 `data/data_v4.db` 的 `personas` 表。  
Session 级别绑定通过 `sp`（preferences 表）的 `session_service_config.persona_id` 实现。

## 网络拓扑

```
Mac mini（AstrBot）
    ↓ 默认网关
路由器 192.168.8.1（Surge TUN）
    ↓
互联网
    ├─ api.sgroup.qq.com（QQ 官方 Bot API）→ DIRECT 规则
    ├─ open.feishu.cn（飞书 API）→ 需稳定路由
    └─ aihubmix / OpenAI / 其他 LLM API → 代理
```

**注意**：QQ 和飞书 API 域名建议在路由器 Surge 配置 DIRECT 规则，避免代理抖动导致连接 reset。

## 主要开发文档索引

| 文档 | 说明 |
|------|------|
| [Router意图路由系统.md](Router意图路由系统.md) | 意图分类规则、配置方式、扩展方法 |
| [Harness马绳任务系统.md](Harness马绳任务系统.md) | 任务生命周期、闭环机制、记忆提升 |
| [HermesBridge集成说明.md](HermesBridge集成说明.md) | Hermes 双向桥接、SessionRouter、回传机制 |
| [新员工引导系统.md](新员工引导系统.md) | ABC 引导插件、角色映射、管理员操作 |
| [运维排障记录.md](运维排障记录.md) | 已修复问题记录、通用排障命令 |
